import React from 'react';
import Input from '../../components/Input/Input';

const Login = () => {
  return (
    <div>
      <Input text='Username' />
      <Input text='Password' />
    </div>
  );
};

export default Login;
